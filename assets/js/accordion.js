const storeAccordions = document.querySelectorAll(".accordion")
const storeAccordionPanels = document.querySelectorAll(".accordion-panel")

for(let storeAccordion of storeAccordions) {
    storeAccordion.addEventListener('click', () => {
        for(let storeAccordionPanel of storeAccordionPanels) {
            storeAccordionPanel.styl
        }
    })
}